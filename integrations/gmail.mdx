---
title: "Gmail"
description: "Connect your Gmail mailbox to make emails searchable in Argos and automatically file relevant threads into matters."
---

The Gmail integration (Google Workspace) connects your mailbox to Argos. Once connected, Argos syncs your email history and automatically queues relevant threads for matter import.

## Connect Gmail

<Steps>
  <Step title="Open Integrations">
    Go to **Settings → Integrations** and click **New Connection**.
  </Step>
  <Step title="Select Gmail">
    Click the **Gmail** tile (labeled *By Argos*).
  </Step>
  <Step title="Authorize with Google">
    You'll be redirected to Google to authorize Argos. Sign in with the Google account whose email you want to connect and grant the requested access.
  </Step>
  <Step title="Watch the sync status">
    After connecting, the connection row shows the sync status: **Sync queued → Sync in progress → Sync completed**. Initial sync may take a few minutes depending on mailbox size.
  </Step>
</Steps>

<Note>
  Each Gmail account can only be connected by one Argos user at a time. If you see *This Gmail account is already connected by another user*, contact your Argos administrator.
</Note>

## Permissions and OAuth scopes

When you authorize Argos, Google asks you to consent to the following Gmail API scopes:

| Consent screen text | Google scope | What it allows |
|---|---|---|
| View your email messages and settings | `gmail.readonly` | Read-only access to view and search your email messages and to view your settings, such as filters and labels. |
| See and edit your email labels | `gmail.labels` | Lets Argos see and edit your Gmail labels so it can organize threads it files. This scope covers labels only — it does not grant access to message content. |

Argos requests `gmail.readonly`, a Google restricted scope, so it can read and index your email messages for search and matter filing. This scope does not let Argos send, modify, or delete email.

Argos also requests `gmail.labels` so it can see and edit Gmail labels used to organize filed threads. This scope does not grant access to message content.

Access is delegated to the signed-in account, so Argos sees only what that account can see.

## What gets synced

The Gmail integration syncs:

- Email threads and individual messages
- Attachments from threads identified as relevant to legal work
- Sync continues automatically — new emails are picked up incrementally after the initial sync

## Automatic matter filing

After syncing, Argos identifies threads that appear relevant to legal work and automatically queues them for matter import. Relevant threads are attached to new or existing matters without manual effort.

{/* VERIFY: customer-facing explanation of what makes a thread "relevant" for matter import — the classification happens internally and no UI copy explaining the criteria was found in code */}

## Disconnecting or deleting

To remove the Gmail connection, go to **Settings → Integrations**, find the connection row, and choose **Disconnect** or **Delete**.

- **Disconnect** pauses syncing but preserves indexed data.
- **Delete** removes the connection entirely. The dialog confirms: *This will delete the connection and disconnect it from the Gmail API*.

{/* VERIFY: whether existing indexed email data is retained or removed after deleting a connection — retention behavior after delete was not confirmed in code */}
